import chai, {expect} from 'chai'
const should = chai.should()
const sinon = require('sinon')

describe('Message Controller', () => {

    context('Get Messages', () => {
    
        it('should receive a list of messages', async () => {

        })
        it('should send a status 500 message when unable to find messages', async () => {

        })
    })
    context('Post Message', () => {

        it('should succesfully post a message', async () => {

        })
        it('should send a status 500 message on unsuccesful post', async () => {
            
        })
    })
})